<header class="mb-auto">
	<div>
		<h3 class="float-md-start mb-0">Demo</h3>
		<nav class="nav nav-masthead justify-content-center float-md-end">
			<a class="nav-link {{app.request.get('_route') == 'home' ? 'active'}}" aria-current="page" href="{{ path('home') }}">Accueil</a>
			{% if is_granted('ROLE_USER') %}
				<a class="nav-link {{app.request.get('_route') == 'newsletter_index' ? 'active'}}" href="{{ path('newsletter_index') }}">Liste des inscrits</a>
				<a class="nav-link" href="{{ path('app_logout') }}">Se déconnecter</a>
			{% else %}
				<a class="nav-link {{app.request.get('_route') == 'login' ? 'active'}}" href="{{ path('login') }}">Connexion</a>
				<a class="nav-link {{app.request.get('_route') == 'register' ? 'active'}}" href="{{ path('register') }}">S'inscrire</a>
			{% endif %}
		</nav>
	</div>
</header>
