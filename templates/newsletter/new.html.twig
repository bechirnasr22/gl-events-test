{% extends 'base.html.twig' %}
{% block title %}Accueil
{% endblock %}
{% block body %}
	<main class="px-3">
		<h1>Renseignez votre email</h1>
		{{ form_start(form) }}
		{{ form_widget(form) }}
		<button class="btn btn-secondary fw-bold border-white bg-white">{{ button_label|default("S'abonner") }}</button>
		{{ form_end(form) }}
	</main>
{% endblock %}
